<%- include('../partials/header') -%>
    <div class="container">
      <div class="row justify-content-center mt-5">
        <div class="col-6">
          <h2><%= wrestler.proName %></h2>
          <img class="img-fluid" src="<%= wrestler.proImage %>" />
          <div class="row justify-content-between">
            <form
              class="col-1"
              action="/wrestlers/follow/<%= wrestler.proName %>?_method=PUT"
              method="POST"
            >
              <button class="btn btn-primary" type="submit">
                <% if (!isFollowing) { %>
                Follow
                <% } else { %>
                Following
                <% } %>
              </button>
            </form>
          </div>
        </div>
        <div class="col-3 mt-5">
          <p>Biography: <%= wrestler.biography %></p>
          <p>Height: <%= wrestler.height %></p>
          <p>Weight: <%= wrestler.weight %></p>
          <% if (wrestler.teammates.length > 0) { %>
          <p>
            Teammates(s): 
            <% for (let i = 0; i < teammates.length; i++) { %>
            <% if (i === teammates.length - 1) { %>
            <%= teammates[i] %>
            <% } else { %>
            <%= teammates[i] %>, 
            <% } %>
            <% } %>
          </p>
          <% } %>
          <% if (wrestler.factions) { %>
          <p>Faction: <%= wrestler.factions %></p>
          <% } %>
          <p>
            Currently with: 
            <% for (let i = 0; i < promos.length; i++) { %>
              <% if (i !== promos.length - 1) { %>
              <a href="/promos/<%= promos[i].promoName %>"><%= promos[i].promoName %></a>, 
              <% } else { %>
              <a href="/promos/<%= promos[i].promoName %>"><%= promos[i].promoName %></a>
              <% } %>
            <% } %>
          </p> 
        </div>
        <div class="col-6 mt-5">
          <a class="btn btn-primary" href="/profile">Return to Profile</a>
          <a class="btn btn-primary" href="/wrestlers">Return to Wrestler List</a>
        </div>
      </div>
    </div>
<%- include('../partials/footer') -%>